# Semantic color system - SPEC

Status: DRAFT (issue #570 / epic #328). This document is the contract shared by the
generative work in `projects/design-tokens` and the semantic work in `projects/styles`.
Numbers marked "proposed" are tuned in the playground (issue #571) before they are frozen.

ASCII only. Arrows are written `->`.

---

## 1. Purpose

Define a color system versatile enough for dense product UIs (GitHub, GitLab class),
that:

- fixes the "too black" dark mode (the canvas is currently pure `#000`);
- gives a real elevation/layering model instead of a flat surface;
- keeps foreground contrast balanced across light and dark;
- stays accessible by construction and verifiable in CI;
- composes with theming (named themes, mode, depth).

Reference systems: GitHub Primer, GitLab Pajamas (elevation + property/role tokens).
daisyUI informs the ergonomics (a theme = a set of semantic variables) but not the
structure (its 3-level `base-100/200/300` is too thin and conflates elevation with
prominence).

## 2. Background - why

Today dark mode is a pure APCA inversion of one generated scale:

- The base surface is the raw primitive `--tone-neutral`, which in dark resolves to
  pure black `#000` (via the `seed` override). The page `body` uses it directly.
- There is no semantic surface layer; page, card, modal, dropdown mostly all map to the
  same `--tone-neutral`. The result is flat-on-black.
- Surfaces come from the neutral tone scale, generated by APCA. APCA clamps low contrast
  near the background, so near-background surface steps are unusable: in dark there is
  nothing between `#000` and `#3c3c3c` (`--tone-neutral-10`). That gap is the mechanism
  behind the "too black" look.

Root causes: (a) the pure-extreme base is unlabeled and overused as a surface; (b) surfaces
are generated by a contrast solver (APCA), which is the wrong tool for near-background
colors.

## 3. Architecture - three layers

```
1. PRIMITIVE   --tone-* (APCA)  |  --surface-* (lightness)  |  status/label/variant/brand (APCA)  |  --tone-*-seed
       |          raw scales, never consumed directly by components
       v
2. SEMANTIC    --magma-surface/text/border/{hue}-*   (property x role x hue, resolved per mode)
       |          the contract; where a theme overrides; bridged to Tailwind --color-*
       v
3. COMPONENT   --mds-<comp>-*  ->  points at the semantic layer   (existing Magma pattern)
```

Pipeline (unchanged): `design-tokens` generates -> `styles` builds and publishes CSS to
`dist/css` -> `stencil` consumes. Per-mode values are published as light-DOM custom
properties on `:root` and flipped under `.pref-theme-dark` / `@media (prefers-color-scheme)`
(the existing `colors-rgb-tones.css` mechanism), so they inherit into every shadow DOM with
no `:host-context` and no Tailwind `dark:` variants.

## 4. Layer 1 - primitives

### 4.1 Foreground tone scale (unchanged)

`--tone-<family>-01..10` for the 5 tone families (porcelain, kaolin, neutral, fireclay,
bisque). Generated by Adobe Leonardo with the APCA (`wcag3`) contrast solver
(`searchColors`). Used for text, icons, borders. NOT changed by this work.

### 4.2 Colored families (unchanged)

`--status-*`, `--label-*`, `--variant-*`, `--brand-*`. APCA. Feed the semantic hue tokens.

### 4.3 Surface scale (NEW)

`--surface-<family>-{sunken,muted,default,raised,overlay}` (5 roles), per mode. Generated by
a direct perceptual-lightness algorithm, NOT a contrast solver (see section 5).

### 4.4 Seed (made explicit)

The pure-extreme base (pure white in light, pure black in dark) is renamed from the bare
`--tone-<family>` to `--tone-<family>-seed` (issue #572, BREAKING). It is an off-scale
escape hatch for the rare case that genuinely wants the pure extreme (scrim, knockout).
It is NOT a surface.

## 5. The two generation engines

Generation splits by ROLE. This is the core decision.

| | Foreground (`tone` / hues) | Surface (`--surface-*`) |
|---|---|---|
| Algorithm | Leonardo + binary search for a CONTRAST target (APCA) | direct LIGHTNESS in OKLCH |
| Input | key color + `ratios` (contrast targets) | key color (tint) + L table per mode/role |
| Library | Leonardo `searchColors` | `chroma-js` directly |
| Determinism | yes | yes (no search) |
| Contrast | generated | verified afterwards (section 9) |

Rationale: APCA is right for foreground (high contrast, cross-mode perceptual parity) and
wrong for surfaces (near-background = low contrast = clamped). Surfaces need controlled
lightness, so `raised` reads a fixed perceptual step above `default` in both modes.

### 5.1 Surface algorithm

```
surface(family, mode, role) = oklch( L[mode][role], C_family * taper(L), H_family )
```

- `L` from the per-mode / per-role table (section 10).
- `C_family`, `H_family` from the family key color (neutral ~= 0 chroma = grayscale;
  porcelain/bisque carry a cool/warm tint used by tinted themes).
- `taper(L)` reduces chroma near the extremes (ininfluent for neutral).

GOTCHA: OKLCH lightness compresses the dark end. `L 16%` is `#0d0d0d` (near black), not
`#161616`. A comfortable dark canvas `#161616` is `L 20%`. Pick by perceptual step and
verify the hex; this is what the playground preview is for.

### 5.2 Config

Per-color opt-in flag + a shared global ramp:

```jsonc
// per color: opt in; the tint comes from the existing `color`
{ "color": "#a3a3a3", "name": "tone.neutral", "seed": { ... }, "surface": true }

// global: the neutral tonal scaffolding - surfaces + borders under ONE `theme` block, so
// they are tuned in relation. `theme.colorspace` (OKLCH) scopes the lightness engine and
// is SEPARATE from the root `colorspace` (HSL) that Leonardo uses for tones/hues.
"theme": {
  "colorspace": "OKLCH",
  "surfaces": {
    "light": { "sunken": "92%", "muted": "94%", "default": "96%", "raised": "99%", "overlay": "99%" },
    "dark":  { "sunken": "15%", "muted": "22%", "default": "20%", "raised": "24%", "overlay": "28%" }
  },
  // borders use the SAME engine; levels sit just OUTSIDE the surface band so one value
  // per mode stays visible on every surface
  "borders": {
    "light": { "muted": "87%", "default": "82%", "strong": "72%" },
    "dark":  { "muted": "30%", "default": "36%", "strong": "44%" }
  }
}
```

`surface: true` opts a family into surface+border generation using `theme.*` levels;
`surface: { levels: {...} }` allows a per-family override later. One family entry now yields
THREE non-colliding outputs: `--tone-<fam>-01..10` (APCA), `--tone-<fam>-seed`,
`--surface-<fam>-*` (+ `--border-<fam>-*`).

Optional 4th border level `stronger` can be added when a concrete element needs it (avoid
speculative levels). A mode-invariant mid-grey (`~L60`, e.g. `"stronger": "60%"` in BOTH
modes) is a valid trick for a border that must stay strong on any background - but note it
does NOT flip per mode (deliberate exception to the flip rule).

### 5.3 Contrast bounds (explicit)

Made explicit in code (issue #578) so out-of-range targets are caught, not silently
clamped:

- `wcag2`: ratio `min 1`, `max 21`.
- `wcag3` (APCA Lc, magnitude; sign flipped in dark): `min 0`, `lowClamp ~8` (below this
  APCA reports 0 - a dead zone), `max ~106` (normal polarity; ~-108 reverse).

The current `wcag3.tone` scale ends at `115` (above the ceiling) so its highest-contrast
step clamps to the pure extreme. Validation flags this.

## 6. Layer 2 - semantic tokens (the contract)

Namespace `--magma-*`. Each is resolved per mode via the global flip. Bridged to Tailwind as
`--color-*` so utilities like `bg-surface-raised`, `text-muted`, `bg-success-surface` exist.

The layer is GENERATED from `../design-tokens/semantic.config.ts` by `scripts/semantic.ts` (A9): that config
is the tracked contract (which primitive each role points at), and `css/semantic.css` +
`tailwind/semantic.css` are generated (gitignored), not hand-edited. Text roles resolve from
the by-target `--text-*` primitives (A7); surfaces/borders through the `--magma-tint-*`
indirection (section 8).

### 6.1 Surfaces - neutral backgrounds (elevation + same-plane prominence)

The `surface` family owns ALL neutral backgrounds - both the elevation axis (z-stacking)
and same-plane prominence (`muted`). 5 roles:

| Token | Axis | Use |
|---|---|---|
| `--magma-surface-sunken` | elevation | wells, insets, code blocks, tracks |
| `--magma-surface-muted` | prominence | same-plane grouping (zebra rows, subtle sections) |
| `--magma-surface-default` | - | canvas / page |
| `--magma-surface-raised` | elevation | cards, panels, sticky headers |
| `--magma-surface-overlay` | elevation | modal, dropdown, popover, tooltip, sheet |

### 6.2 Text (prominence)

`--magma-text-{default,muted,subtle,disabled,on-emphasis}`

### 6.3 Border (prominence)

`--magma-border-{muted,default,strong,focus}` - 4 roles by prominence, NOT indexed per
surface level.

- OPAQUE, explicit value per mode (light and dark), flipping via the global mechanism -
  never a single alpha, so they never vanish in dark. Alpha stays for shadow/outline only.
- Generated by the SAME OKLCH lightness-levels engine as surfaces (`theme.borders` levels,
  section 5.2), so borders and surfaces share one mechanism, are tuned in relation, and stay
  out of the APCA domain.
- The border levels sit just OUTSIDE the surface band (lighter than every surface in dark,
  darker than every surface in light), so a SINGLE value per mode contrasts against ALL
  surface levels - no contextual rework, and contrast is never lost in dark.
- CAVEAT (the real risk): an absolute border level placed INSIDE the surface band would
  vanish on the surface at the same L (e.g. an overlay). Placing it beyond the band avoids
  this; in practice borders live on canvas/cells while overlays use shadow, not borders.
  This is what GitHub does (one `borderColor.muted`).
- `muted` (decorative: grid, dividers, cell/row borders) is the softest; `default` a touch
  stronger (functional outlines: inputs). Decorative borders have NO 3:1 floor (WCAG 1.4.11
  does not require it for dividers whose separation is already conveyed by layout) - the
  lever against the over-contrasted grid.
- `strong` is the assertive neutral (state); `focus` is an accent color (not a neutral
  level), verified on the surfaces where it appears.

### 6.4 Hues (cross-cutting)

For each of `neutral, accent, info, success, warning, danger`, the same quintet:

| Token | Use |
|---|---|
| `--magma-<hue>-surface` | subtle tinted fill (banner/badge/alert background) |
| `--magma-<hue>-fg` | colored text/icon |
| `--magma-<hue>-border` | colored border |
| `--magma-<hue>-emphasis` | solid fill (primary button, solid badge) |
| `--magma-<hue>-on-emphasis` | text on the solid fill |

The `neutral` hue is special: its background needs are already covered by the `surface`
family (its "surface" role IS `surface-muted` / `surface-default`), so in the hue matrix
`neutral` contributes only `fg` / `border` / `emphasis` / `on-emphasis` (text, borders, the
dark neutral chip). The colored hues (accent/info/success/warning/danger) carry the full
quintet. This keeps neutral backgrounds in ONE place (the `surface` family) with no
duplicate `neutral-surface` token.

### 6.5 Source mapping

| Semantic | Source primitive |
|---|---|
| `surface-*` (5) | `--surface-<active-tint>-*` (lightness); active tint = neutral by default (section 8) |
| `text-*` | `--tone-neutral-*` (APCA) |
| `border-{muted,default,strong}` | `--border-<active-tint>-*` (OKLCH levels, opaque, per mode, beyond the surface band); `focus` = accent |
| `<hue>-fg / -border / -emphasis` | `status/label/variant-*` (APCA) |
| `<hue>-surface` | subtle end (step 09/10) of the colored family |
| `*-on-emphasis` | `--tone-*-seed` / near-extreme, verified on the fill |

### 6.6 Interaction states

Do NOT author hover/active/selected as new tokens. Derive them (one elevation step, or
`color-mix` on the base token). Focus uses `--magma-border-focus`.

## 7. Layer 3 - component tokens

Components keep their existing `--mds-<comp>-*` custom properties and change only the
DEFAULT to point at a semantic role. Example:

```css
/* before */ .card { background: rgb(var(--tone-neutral)); }
/* after  */ .card { background: var(--magma-surface-raised); }
```

Role mapping: page/body -> `surface-default`; card/panel -> `surface-raised`;
modal/dropdown/popover/tooltip -> `surface-overlay`; well/inset -> `surface-sunken`.
Colored/status components keep their tonal background (from `status`/`label`) and are only
verified against the new surfaces. Consumer overrides of the component token keep working.

## 8. Theming axes

Three independent axes, all driven by classes/attributes on `<html>` (cross-browser; no
`@container style()`, which is below the browserslist floor and crashes WebKit with shadow
DOM). Same mechanism as the existing preference system.

- `data-theme-name` (attribute): a named theme OVERRIDES the semantic layer. The active
  surface tint is an indirection: `--magma-surface-*` resolve from an active-tint pointer
  (`--magma-tint-*`) that defaults to the `neutral` family; a theme repoints it to another
  family (e.g. `porcelain` for a cool, GitLab-like dark) - one swap, no per-token edits.
  Same pattern for the accent hue. (Note: this is a semantic indirection; the config
  `alias` field is declared but NOT consumed by the generator - `variant-primary` and
  `brand-maggioli` merely share a seed, they are not live-aliased.) A theme is an override
  map of the semantic tokens (daisyUI-style ergonomics).
- mode `light | dark | system`: the existing global flip; semantic tokens follow it.
- `--depth` (numeric `0 | 1`): shadow/bevel intensity, consumed as a scalar in `calc()`
  (NOT a `true|false` style query). Does not change the surface colors; only the shadow
  treatment on top.

The draft `projects/styles/draft/theme.css` (`--tint-base-lv*`) is superseded by this
spec: `lv1/lv2/lv3` -> the role names in 6.1, sourced from `--surface-*` (not the clamped
tone steps).

## 9. Accessibility - verification and de-emphasis

Contrast is not imposed on surfaces; it is chosen (by lightness) and VERIFIED.

### 9.1 Text targets (verification thresholds, NOT changes to the 10-step scale)

The foreground tone scale is unchanged. These are the thresholds the CI gate (issue #575)
checks when existing text sits on the NEW surfaces, in both modes. Split by essential vs
non-essential, because APCA floors differ:

| Role | Target Lc | Notes |
|---|---|---|
| `text-default` | ~85 (min 75) | body; primary data |
| `text-muted` | ~75 (min 75) | secondary ESSENTIAL text (address, phone) - stays legible |
| `text-subtle` | ~45 | NON-essential only (caption, unit, hint) - below body floor is allowed |
| `text-disabled` | ~30 | disabled (non-essential) |
| `*-on-emphasis` | >= 75 | text on a solid fill |

Verified pairs: every `text-*` on every `surface-*`; every `<hue>-fg` on
`surface-default`/`-raised`; every `<hue>-on-emphasis` on `<hue>-emphasis`. Failure prints
a table of offending pairs (Lc vs target) - GitHub's per-PR check applied to the semantic
pairs.

### 9.2 De-emphasis without dropping text contrast

Essential text cannot drop below its floor just to look lighter (that was the old WCAG2
under-contrast that also broke in dark). Build hierarchy with OTHER axes:

- font WEIGHT (primary medium/semibold, secondary regular);
- CHROME contrast, lowered hard: table cell/row borders are decorative -> `border-muted`,
  soft (no 3:1 floor). This is the main lever against the "schematic spreadsheet" look that
  APCA introduced on the grid;
- prefer ZEBRA (`surface-muted`) over grid lines where borders are not needed (GitLab's
  "depth without heavy borders");
- spacing and size.

Only borders that convey STATE (selected, focus, error) stay at full contrast
(`border-strong` / `border-focus`).

## 10. Baseline surface L-table (proposed)

Neutral family (C = 0), computed with the repo's chroma-js + apca-w3. Starting point;
tuned in the playground. Default text = `#e9e9e9` (dark) / `#404040` (light).

| Role | DARK  L / hex / Lc | LIGHT  L / hex / Lc |
|---|---|---|
| `sunken` | 15% / `#0b0b0b` / 93 | 92% / `#e6e6e6` / ~79 |
| `default` | 20% / `#161616` / 93 | 96% / `#f2f2f2` / 86 |
| `muted` | 22% / `#1b1b1b` / 92 | 94% / `#ebebeb` / 82 |
| `raised` | 24% / `#1f1f1f` / 92 | 99% / `#fcfcfc` / 92 |
| `overlay` | 28% / `#292929` / 90 | 99% / `#fcfcfc` + shadow / 92 |

Borders (opaque OKLCH levels, per mode, placed just outside the surface band; proposed):

| Role | DARK L / hex | LIGHT L / hex |
|---|---|---|
| `border-muted` | 30% / `#2e2e2e` | 87% / `#dbdbdb` |
| `border-default` | 36% / `#3a3a3a` | 82% / `#cfcfcf` |
| `border-strong` | 44% / `#525252` | 72% / `#b0b0b0` |

Dark borders are lighter than every surface (top = overlay L28); light borders are darker
than every surface (bottom = sunken L92) - so one value per mode is visible everywhere.

Notes:

- NO pure `#fff` / `#000` anywhere in the surface or border ladders - those live ONLY in
  `--tone-*-seed`.
- Dark elevation is fill-led (lighter = higher). Light combines a slightly grey canvas
  (card `#fcfcfc` pops on canvas `#f2f2f2`) with the `--depth` shadow; near-white fill
  steps alone would be imperceptible.
- `muted` is a same-plane grouping step: darker than canvas in light, lighter in dark.
- Text stays Lc >= ~79 across the ladder (body target ~75), with headroom to retune.

## 11. Naming rationale

Role-based (`surface-raised`, `text-muted`, `success-emphasis`), not ordinal
(`base-100/200/300`). Reasons: self-documenting utility classes; elevation and prominence
are kept on separate axes (daisyUI conflates them); extensible to the full property x role
x hue matrix that dense apps need. `-content`-style foreground is expressed as
`text-*` / `<hue>-fg` / `*-on-emphasis`.

### Namespace convention (prefixes by ownership)

Three CSS-variable prefixes, kept distinct so they never collide:

| Prefix | Owner | What it holds | Example |
|---|---|---|---|
| `--tone-* / --surface-* / --status-* / --border-* / ...` | design-tokens | generated PRIMITIVES (raw scales) | `--surface-neutral-default` |
| `--magma-*` | styles | everything STYLES owns: the semantic color layer AND the global utilities (prefs, z-index, opacity, focus) | `--magma-surface-raised`, `--magma-text-muted`, `--magma-modal-z-index` |
| `--mds-<comp>-*` | components | per-component API (the knobs a consumer overrides) | `--mds-button-background` |

The semantic layer is `--magma-*`, NOT `--mds-*`: it is authored and owned by
`styles`, exactly like the other `--magma-*` globals (see `styles/SPEC.md`).
`--mds-*` stays reserved for component tokens, so the semantic role
`--magma-text-muted` never collides with a component prop like
`--mds-text-selection-color` (from the `mds-text` component). A component adopts
the system by pointing its `--mds-<comp>-*` default at a `--magma-*` role, e.g.
`--mds-card-background: var(--magma-surface-raised)`.

## 12. Migration

- `--tone-<family>` (bare) -> `--tone-<family>-seed` (issue #572). ~243 uses of
  `--tone-neutral` as a background must move to semantic surfaces.
- Migration is manifest-driven via the existing `projects/codemod` surfaces framework
  (issue #576): a `cssVarRename` for the seed, plus a triaged repoint of background
  usages to surface roles (ambiguous sites reported, not blind-rewritten).
- Component adoption + a semantic-layout Storybook story (surface ladder x components,
  light and dark) with the a11y addon as the gate (issue #577).

## 13. Documentation and distribution

Two kinds of documentation, kept apart:

- INTERNAL design SPEC (rationale, algorithm, decisions, issue map): stays in the source
  tree, NOT shipped. Split by ownership:
  - generative half (surface engine, config, contrast bounds - sections 4.3, 5) ->
    `projects/design-tokens/SPEC.md`;
  - semantic + contract + theming half (sections 3, 6-9, 11) -> this document in
    `projects/styles/`.
  - Cross-linked; each project documents what it owns.
- CONSUMER reference (agent-readable usage, shipped to dist so a vendor's agent can read
  it from `node_modules`): the tokens that exist and how to use them - NOT the rationale.

Consumer references are authored in the single source `docs/agents/` and shipped by the
existing `sync-agent-docs.ts` mechanism (today it stamps `AGENTS.md` + `agents/` into the
3 entry-point packages only). TWO tracks, each shipped only into its owning package so a
consumer never carries docs it does not need:

| Track | Ships into | Covers |
|---|---|---|
| `docs/agents/tokens.md` | `@maggioli-design-system/design-tokens` | primitives (`--tone-*`, `--surface-*`, `--*-seed`, colored families), the contrast bounds, how to consume the CSS vars |
| `docs/agents/theming.md` | `@maggioli-design-system/styles` | the semantic layer (`--magma-*` roles + when to use each), the Tailwind utilities, and the theming attributes (`data-theme-name`, `--depth`, mode) |

Extending shipping to `design-tokens` and `styles` reverses (for this material) the earlier
"3 entry-points only" decision. Tracked in issue A6 (#581).

## 14. Issue map

Epic: #328.

- #570 S0 - this spec
- #571 A1 - surface lightness scale + playground editing/preview
- #572 A2 - seed rename (BREAKING)
- #573 B1 - semantic layer + Tailwind bridge
- #574 B2 - theming axes
- #575 A3 - APCA contrast gate in CI
- #576 C1 - codemod manifest rules
- #577 C2 - component adoption + story + a11y gate
- #578 A5 - explicit contrast bounds (constants + validation + playground band)
- #581 A6 - ship token + theming agent-docs into design-tokens and styles dist

## 15. Resolved decisions

- Surface levels: 5 - `sunken`, `muted`, `default`, `raised`, `overlay`. `muted` is the
  same-plane grouping fill (a dedicated surface role, not folded into a hue).
- Light canvas: NOT pure white. Grey canvas (`~L96`) with near-white raised (`~L99`, not
  `#fff`). Pure `#fff` / `#000` live only in `--tone-*-seed`.
- Default tint: `neutral`, swappable via the `--magma-tint-*` indirection (section 8).
- Text APCA targets: section 9.1, split essential vs non-essential. The 10-step foreground
  scale is UNCHANGED; these are verification thresholds only.
- Borders: opaque, explicit per mode, delta from surface, one value per mode (section 6.3);
  never a single vanishing alpha.
- Table "gabbia": decorative borders -> `border-muted` (soft, no 3:1 floor) or zebra;
  only state borders stay full contrast (section 9.2).

Remaining to tune in the playground (issue #571): the exact L / hex / border-delta values
in section 10.
