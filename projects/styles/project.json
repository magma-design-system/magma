{
  "name": "styles",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "type": "library",
  "tags": ["type:lib"],
  "targets": {
    "build": {
      "dependsOn": ["^build", "design-tokens:build"],
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/projects/stencil/src/components/**/*-property.css"
      ]
    }
  }
}
