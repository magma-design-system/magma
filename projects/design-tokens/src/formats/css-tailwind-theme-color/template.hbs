/*
@magma-design-system/design-tokens v{{ version }}
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

@theme {
  {{!-- {{#each properties.}} --}}
  {{#each properties as | groups |}}
    {{#each groups as | names |}}
      {{#each names as | lightModes |}}
        {{#each lightModes as | palettes |}}
          {{#ifEquals @key 'light'}}
            {{#each palettes as | palette |}}
              {{#ifEquals @key 'color'}}
  --color-{{@../../../key}}-{{@../../key}}: rgb(var(--{{@../../../key}}-{{@../../key}}));
              {{else}}
  --color-{{@../../../key}}-{{@../../key}}-{{leadZero @key}}: rgb(var(--{{@../../../key}}-{{@../../key}}-{{leadZero @key}}));
              {{/ifEquals}}
            {{/each}}
          {{/ifEquals}}
          {{/each}}
      {{/each}}
    {{/each}}
  {{/each}}
}
{{!--
:root.pref-theme-dark {
  {{#each properties as | groups |}}
    {{#each groups as | names |}}
      {{#each names as | lightModes |}}
        {{#each lightModes as | palettes |}}
          {{#ifEquals @key 'dark'}}
            {{#each palettes as | palette |}}
              {{#ifEquals @key 'color'}}
  --{{@../../../key}}-{{@../../key}}: {{rgbChannel palette.value}};
              {{else}}
  --{{@../../../key}}-{{@../../key}}-{{leadZero @key}}: {{rgbChannel palette.value}};
              {{/ifEquals}}
            {{/each}}
          {{/ifEquals}}
          {{/each}}
      {{/each}}
    {{/each}}
  {{/each}}
}

@media (prefers-color-scheme: dark) {
  :root.pref-theme-system {
    {{#each properties as | groups |}}
      {{#each groups as | names |}}
        {{#each names as | lightModes |}}
          {{#each lightModes as | palettes |}}
            {{#ifEquals @key 'dark'}}
              {{#each palettes as | palette |}}
                {{#ifEquals @key 'color'}}
    --{{@../../../key}}-{{@../../key}}: {{rgbChannel palette.value}};
                {{else}}
    --{{@../../../key}}-{{@../../key}}-{{leadZero @key}}: {{rgbChannel palette.value}};
                {{/ifEquals}}
              {{/each}}
            {{/ifEquals}}
            {{/each}}
        {{/each}}
      {{/each}}
    {{/each}}
  } --}}

