/*
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

const getColor = (cssVar, { opacityVariable, opacityValue }) => {
  if (opacityValue !== undefined) {
    return `rgb(var(--${cssVar}) / ${opacityValue})`
  }
  if (opacityVariable !== undefined) {
    return `rgb(var(--${cssVar}) / var(${opacityVariable}, 1))`
  }
  return `rgb(var(--${cssVar}))`
}

const palette = {
  {{#each properties as | groups |}}
    {{#each groups as | names |}}
      {{#each names as | lightModes |}}
        {{#each lightModes as | palettes |}}
          {{#ifEquals @key 'light'}}
            {{#each palettes as | palette |}}
              {{#ifEquals @key 'color'}}
  '{{@../../../key}}-{{@../../key}}': params => getColor('{{@../../../key}}-{{@../../key}}', params),
              {{else}}
  '{{@../../../key}}-{{@../../key}}-{{leadZero @key}}': params => getColor('{{@../../../key}}-{{@../../key}}-{{leadZero @key}}', params),
              {{/ifEquals}}
            {{/each}}
          {{/ifEquals}}
          {{/each}}
      {{/each}}
    {{/each}}
  {{/each}}
}

module.exports = {
  palette,
}
