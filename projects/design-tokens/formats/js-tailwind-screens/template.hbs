/*
@magma-design-system/design-tokens v{{ version }}
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

const screens = {
{{#each properties as | viewports |}}
  {{#each viewports as | viewport |}}
  {{#ifEquals @key 'mobile'}}
  {{@key}}: {
    {{#each viewport as | rule |}}
      {{#ifEquals @key 'max'}}
      {{@key}}: '{{rule.value}}',
      {{/ifEquals}}
    {{/each}}
  },
  {{else}}
  {{@key}}: {
    {{#each viewport as | rule |}}
      {{#ifEquals @key 'min'}}
      {{@key}}: '{{rule.value}}',
      {{/ifEquals}}
    {{/each}}
  },
  {{/ifEquals}}
  {{#each viewport as | rule |}}
  {{#ifEquals @../key 'mobile'}}
  {{else}}
  {{#ifEquals @key 'max'}}
  '{{@../key}}-{{@key}}': {
    {{@key}}: '{{rule.value}}',
  },
  {{/ifEquals}}
  {{/ifEquals}}
  {{/each}}
  {{/each}}
{{/each}}
}

module.exports = {
  screens,
}
