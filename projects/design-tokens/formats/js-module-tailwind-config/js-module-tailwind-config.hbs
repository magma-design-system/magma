/*
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

const palette = {
  {{#each properties as | groups |}}
    {{#each groups as | names |}}
      {{#each names as | lightModes |}}
        {{#each lightModes as | palettes |}}
          {{#ifEquals @key 'light'}}
            {{#each palettes as | palette |}}
              {{#ifEquals @key 'color'}}
    '{{@../../../key}}-{{@../../key}}': ({ opacityVariable, opacityValue }) => {
      if (opacityValue !== undefined) {
        return `rgba(var(--{{@../../../key}}-{{@../../key}}), ${opacityValue})`
      }
      if (opacityVariable !== undefined) {
        return `rgba(var(--{{@../../../key}}-{{@../../key}}), var(${opacityVariable}, 1))`
      }
      return `rgb(var(--{{@../../../key}}-{{@../../key}}))`
    },
              {{else}}
    '{{@../../../key}}-{{@../../key}}-{{leadZero @key}}': ({ opacityVariable, opacityValue }) => {
      if (opacityValue !== undefined) {
        return `rgba(var(--{{@../../../key}}-{{@../../key}}-{{leadZero @key}}), ${opacityValue})`
      }
      if (opacityVariable !== undefined) {
        return `rgba(var(--{{@../../../key}}-{{@../../key}}-{{leadZero @key}}), var(${opacityVariable}, 1))`
      }
      return `rgb(var(--{{@../../../key}}-{{@../../key}}-{{leadZero @key}}))`
    },
              {{/ifEquals}}
            {{/each}}
          {{/ifEquals}}
          {{/each}}
      {{/each}}
    {{/each}}
  {{/each}}
}

module.exports = {
  palette,
}
