/*
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

const palette = {
  {{#each properties as | groups |}}
    {{#each groups as | names |}}
      {{#each names as | lightModes |}}
  '{{@../key}}-{{@key}}': {
        {{#each lightModes as | palettes |}}
          {{#ifEquals @key 'light'}}
            {{#each palettes as | palette |}}
              {{#ifEquals @key 'color'}}
    'DEFAULT': 'var(--{{@../../../key}}-{{@../../key}})'{{#unless @last}},{{/unless}}
              {{else}}
    '{{@key}}': 'var(--{{@../../../key}}-{{@../../key}}-{{@key}})'{{#unless @last}},{{/unless}}
              {{/ifEquals}}
            {{/each}}
          {{/ifEquals}}
          {{/each}}
  },
      {{/each}}
    {{/each}}
  {{/each}}
}

module.exports = {
  palette,
}
