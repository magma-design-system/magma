<!--
================================================================================
AUTHORING RULES — description.md
================================================================================
Purpose: explain WHAT this component is and what each prop MEANS.
         The reader is an AI agent or a developer deciding whether to use it.

Companion docs to read first (do not re-derive their content here):
  - docs/COMPONENTS.md       — the usage/ contract and the cross-cutting
                               variant/tone/size ladders shared across components.
  - projects/stencil/SPEC.md — registration, naming, events, slots, dictionaries.

MUST contain (in this exact order):
  1. ONE opening paragraph (1–2 sentences, no heading). Reference the tag
     literally — e.g. `<{{componentName}}>` — and place the component in the
     system: what role it plays, what HTML primitive it replaces or extends.
  2. A "## Semantic Behavior" section: intrinsic, runtime behavior the
     component encapsulates — behaviors that survive across themes, sizes,
     and variants. Bulleted, one behavior per bullet, **bold lead** + one
     sentence. Cover state (`active`, `disabled`, `await`), form association,
     role/ARIA defaults, focus, async handling, etc.
  3. A "## Properties & Visual Configurations" section: the INTENT of each
     prop whose meaning isn't obvious from its name — when to pick
     `tone="strong"` vs `tone="weak"`, what `variant` families exist, what
     `size` controls. Group sub-options with nested bullets ONLY when each
     value carries a distinct semantic role worth naming.

MUST NOT contain:
  - The full props table or the raw type signature (lives in `readme.md`,
    auto-generated from `@Prop()` JSDoc).
  - Exhaustive lists of allowed string values (lives in `components.d.ts`
    and `src/type/*.ts` — those are the source of truth).
  - Code examples — those belong in `pattern.md`.
  - Anti-patterns or "don't do X" — those belong in `antipattern.md`.
  - Generic stencil rules that apply to every component — those live in
    `projects/stencil/SPEC.md` and `docs/COMPONENTS.md`.
  - Trivial props (`label`, `name`, `id`, `autoFocus`) whose meaning is
    obvious from the name.

STYLE & LENGTH:
  - Prose first, then bulleted lists. Each prop entry: `**propName**:` + a
    one-sentence intent, then a nested sub-list only if values diverge
    semantically.
  - Target 20–40 lines total. Longer means you're describing implementation
    rather than usage — trim.
  - Reference props by their camelCase TS name (`iconPosition`, not
    `icon-position`); values stay in backticks (`'strong'`, `'weak'`).
  - Delete this comment block before committing.
================================================================================
-->

The `<{{componentName}}>` web component <!-- one or two sentences: what is it, and what role does it play in the system? Mention the HTML primitive it replaces or extends, if any. -->.

## Semantic Behavior

<!--
Describe runtime behavior that a developer can't infer from the props table.
Examples of what belongs here:
  - "Renders as button by default; becomes a link when `href` is set."
  - "Form-associated; participates in native submit/reset."
  - "Disabled state removes it from the tab sequence."
  - "Await state maps to `aria-busy='true'` and intercepts clicks."
  - "Derives `aria-label` from the `label` prop or slotted text."
Bullet list. One behavior per bullet. Bold lead naming the trigger / state.
-->

- **<!-- behavior name -->**: <!-- one-sentence description -->.
- **<!-- behavior name -->**: <!-- one-sentence description -->.

## Properties & Visual Configurations

<!--
Document the INTENT of each prop — when to pick which value, not the value list.

Group conventions to follow:
  - For `variant`, group values into semantic families (Brand / Status /
    Luminance / Identity / Label) — see docs/COMPONENTS.md §variant.
  - For `tone`, name each value's emphasis level — see docs/COMPONENTS.md §tone.
  - For free-form props (strings, numbers), one line is enough.

Skip any prop whose meaning is obvious from its name (`label`, `name`, `id`,
`autoFocus`, etc.). Skip props already fully covered in Semantic Behavior
(`disabled`, `await`, `href`) unless they have visual configurations worth
naming.
-->

- **`<!-- propName -->`**: <!-- intent — one sentence. If values have distinct meanings, list them below. -->
  - `<!-- value -->`: <!-- when to use it -->.
  - `<!-- value -->`: <!-- when to use it -->.
- **`<!-- propName -->`**: <!-- intent -->.
