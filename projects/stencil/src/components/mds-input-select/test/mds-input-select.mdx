import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Form / Select / Caveats" />

## React Caveats

When using a component on react if you want to use a default value you must use the `default-value` attribute and not `defaultValue` or `value`
