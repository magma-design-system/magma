.base-table-header:
  variables:
    COMPONENT: mds-table-header

# ISOLATE
table-header-isolate:
  extends: [.base-isolate, .base-table-header]
  dependencies: [base-stencil-build]

# PUBLISH
table-header-publish:
  extends: [.base-stencil-publish, .base-table-header]
  dependencies: [base-stencil-build, table-header-isolate]
  needs: [base-stencil-build, table-header-isolate, table-header-publish-test]

# INSTALL TEST
table-header-install-test:
  extends: [.base-stencil-install-test, .base-table-header]
  dependencies: [base-stencil-build, table-header-isolate]
  needs: [base-stencil-build, table-header-isolate, table-header-publish]
