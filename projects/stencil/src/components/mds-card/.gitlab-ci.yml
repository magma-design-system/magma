.base-card:
  variables:
    COMPONENT: mds-card

# ISOLATE
card-isolate:
  extends: [.base-isolate, .base-card]
  dependencies: [base-build]

# TEST
card-publish-test:
  extends: [.base-publish-test, .base-card]
  dependencies: [base-build, card-isolate]

# PUBLISH
card-publish:
  extends: [.base-publish, .base-card]
  dependencies: [base-build, card-isolate]
