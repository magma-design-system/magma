.base-help:
  variables:
    COMPONENT: mds-help

# TEST
help-publish-test:
  extends: [.base-stencil-publish-test, .base-help]
  dependencies: [base-stencil-build, base-isolate]

# PUBLISH
help-publish:
  extends: [.base-stencil-publish, .base-help]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, help-publish-test]

# INSTALL TEST
help-install-test:
  extends: [.base-stencil-install-test, .base-help]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, help-publish]
