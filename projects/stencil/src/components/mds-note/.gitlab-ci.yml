.base-note:
  variables:
    COMPONENT: mds-note

# ISOLATE
note-isolate:
  extends: [.base-isolate, .base-note]
  dependencies: [base-build]

# TEST
note-publish-test:
  extends: [.base-publish-test, .base-note]
  dependencies: [base-build, note-isolate]

# PUBLISH
note-publish:
  extends: [.base-publish, .base-note]
  dependencies: [base-build, note-isolate]
