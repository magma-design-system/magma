import { Canvas, Meta, Story } from "@storybook/addon-docs";

<Meta title="Form / Input / Known issues" />

# Known issues

---

### Figma differences

At the moment there are differences between Figma and the component. We are waiting to refine Figma component proposed by Luca Pernechele and to see if we can get the same result on Figma.

### Right icon for special input types

- The height of the component changes
- The icon should be controlled by the Design System instead of OS
