.base-breadcrumb:
  variables:
    COMPONENT: mds-breadcrumb

# ISOLATE
breadcrumb-isolate:
  extends: [.base-isolate, .base-breadcrumb]
  dependencies: [base-stencil-build]

# TEST
breadcrumb-publish-test:
  extends: [.base-stencil-publish-test, .base-breadcrumb]
  dependencies: [base-stencil-build, breadcrumb-isolate]

# PUBLISH
breadcrumb-publish:
  extends: [.base-stencil-publish, .base-breadcrumb]
  dependencies: [base-stencil-build, breadcrumb-isolate]
  needs: [base-stencil-build, breadcrumb-isolate, breadcrumb-publish-test]

# INSTALL TEST
breadcrumb-install-test:
  extends: [.base-stencil-install-test, .base-breadcrumb]
  dependencies: [breadcrumb-publish, breadcrumb-isolate]
