.base-spinner:
  variables:
    COMPONENT: mds-spinner

# ISOLATE
spinner-isolate:
  extends: [.base-isolate, .base-spinner]
  dependencies: [base-stencil-build]

# PUBLISH
spinner-publish:
  extends: [.base-stencil-publish, .base-spinner]
  dependencies: [base-stencil-build, spinner-isolate]
  needs: [base-stencil-build, spinner-isolate, spinner-publish-test]

# INSTALL TEST
spinner-install-test:
  extends: [.base-stencil-install-test, .base-spinner]
  dependencies: [base-stencil-build, spinner-isolate]
  needs: [base-stencil-build, spinner-isolate, spinner-publish]
