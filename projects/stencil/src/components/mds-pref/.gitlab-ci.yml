.base-pref:
  variables:
    COMPONENT: mds-pref

# TEST
pref-publish-test:
  extends: [.base-stencil-publish-test, .base-pref]
  dependencies: [base-stencil-build, base-isolate]

# PUBLISH
pref-publish:
  extends: [.base-stencil-publish, .base-pref]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, pref-publish-test]

# INSTALL TEST
pref-install-test:
  extends: [.base-stencil-install-test, .base-pref]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, pref-publish]
