.base-table-cell:
  variables:
    COMPONENT: mds-table-cell

# ISOLATE
table-cell-isolate:
  extends: [.base-isolate, .base-table-cell]
  dependencies: [base-stencil-build]

# TEST
table-cell-publish-test:
  extends: [.base-stencil-publish-test, .base-table-cell]
  dependencies: [base-stencil-build, table-cell-isolate]

# PUBLISH
table-cell-publish:
  extends: [.base-stencil-publish, .base-table-cell]
  dependencies: [base-stencil-build, table-cell-isolate]
  needs: [base-stencil-build, table-cell-isolate, table-cell-publish-test]

# INSTALL TEST
table-cell-install-test:
  extends: [.base-stencil-install-test, .base-table-cell]
  dependencies: [base-stencil-build, table-cell-isolate]
  needs: [base-stencil-build, table-cell-isolate, table-cell-publish]
