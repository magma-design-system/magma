.base-zero:
  variables:
    COMPONENT: mds-zero

# TEST
zero-publish-test:
  extends: [.base-stencil-publish-test, .base-zero]
  dependencies: [base-stencil-build, base-isolate]

# PUBLISH
zero-publish:
  extends: [.base-stencil-publish, .base-zero]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, zero-publish-test]

# INSTALL TEST
zero-install-test:
  extends: [.base-stencil-install-test, .base-zero]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, zero-publish]
