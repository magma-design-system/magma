.base-flex-table:
  variables:
    COMPONENT: mds-flex-table

# ISOLATE
flex-table-isolate:
  extends: [.base-isolate, .base-flex-table]
  dependencies: [base-stencil-build]

# TEST
flex-table-publish-test:
  extends: [.base-stencil-publish-test, .base-flex-table]
  dependencies: [base-stencil-build, flex-table-isolate]

# PUBLISH
flex-table-publish:
  extends: [.base-stencil-publish, .base-flex-table]
  dependencies: [base-stencil-build, flex-table-isolate]
