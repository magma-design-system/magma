.base-input-date-range:
  variables:
    COMPONENT: mds-input-date-range

# TEST
input-date-range-publish-test:
  extends: [.base-stencil-publish-test, .base-input-date-range]
  dependencies: [base-stencil-build, base-isolate]

# PUBLISH
input-date-range-publish:
  extends: [.base-stencil-publish, .base-input-date-range]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, input-date-range-publish-test]

# INSTALL TEST
input-date-range-install-test:
  extends: [.base-stencil-install-test, .base-input-date-range]
  dependencies: [base-stencil-build, base-isolate]
  needs: [base-stencil-build, base-isolate, input-date-range-publish]
