.base-avatar:
  variables:
    COMPONENT: mds-avatar

# ISOLATE
avatar-isolate:
  extends: [.base-isolate, .base-avatar]
  dependencies: [base-build]

# TEST
avatar-publish-test:
  extends: [.base-publish-test, .base-avatar]
  dependencies: [base-build, avatar-isolate]

# PUBLISH
avatar-publish:
  extends: [.base-publish, .base-avatar]
  dependencies: [base-build, avatar-isolate]
