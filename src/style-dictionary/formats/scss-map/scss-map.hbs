/*
Generated by Style Dictionary
Do not edit directly
Generated at {{ date }}
*/

{{#each properties as | types |}}
  {{#each types as | items |}}
${{@../key}}-{{@key}}: (
    {{#each items as | item |}}
  '{{@key}}': (
        {{#each item as | subItem |}}
    '{{@key}}': {{subItem.value}}{{#unless @last}},{{/unless}}
        {{/each}}
  ){{#unless @last}},{{/unless}}
    {{/each}}
);
  {{/each}}
{{/each}}
