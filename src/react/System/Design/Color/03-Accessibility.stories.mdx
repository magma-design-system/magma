import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import faker from 'faker'

import Table, { TableHeader, TableHeaderCell, TableBody } from '@Layout/Table/Table'
import AccessibilitySet from '@System/Design/Color/AccessibilitySet'
import Tokens from '$Tokens/colors.json'



<Meta title="Design/Colors/Accessibility" component={AccessibilitySet} />

### Seeds palette

To ease the process of making the color palette consistent, tones will be the key to modulate Status and Brand tints with a tone matrix that will be used to adjust their tones when the UI needs a different tone of the same tint seed.

<Table>
  <TableHeader>
    <TableHeaderCell>Color</TableHeaderCell>
    <TableHeaderCell>Name</TableHeaderCell>
    <TableHeaderCell>Example</TableHeaderCell>
    <TableHeaderCell>Accessibility</TableHeaderCell>
  </TableHeader>
  <TableBody>
    <AccessibilitySet colors={Tokens.tint}/>
    <AccessibilitySet colors={Tokens.tone}/>
  </TableBody>
</Table>

### Accessibility test

Use this tool to find the better accessibility for your contents

